# BOOTSTRAP -- First-run checklist (delete after setup)

## Phase 0 Mode
- Tools = web search only.
- No heartbeats unless explicitly enabled.
- Outputs must be structured (see AGENTS.md default format).

## What Mark should paste into the chat (minimum viable context)
1) Current shows / exhibitions (names + dates)
2) Current pipeline snapshot (leads + stage + last touch)
3) Top 3 operational pain points this week
4) Any "must do" deadlines (rent, events, shipments)

## Success criteria (Phase 0)
- Produces a usable weekly ED packet from pasted inputs.
- Produces follow-up queue + drafts without inventing facts.
- Uses web search with citations only when requested.

Once working:
- Delete this BOOTSTRAP.md (it should not reappear unless workspace is reset)
